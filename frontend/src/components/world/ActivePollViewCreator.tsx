import { Modal } from "@chakra-ui/react";
import { ActivePollViewProps } from "./ActivePollView";

export default function ActivePollViewCreator(apvProps: ActivePollViewProps) {
    return <Modal />
}