import { PollViewProps } from "./ActivePollView";

export default function ExpiredPollView(apvProps: PollViewProps): JSX.Element {
    // just placeholder atm, James should implement the actual expired poll view
    return <text>bunger</text>
}